package org.example.controller;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * Plain Spring MVC (non-Boot) demo.
 *
 * Integration tests would start the embedded Tomcat from {@code org.example.App}
 * and then issue HTTP calls.
 */
class HelloControllerTest {

    @Test
    @Disabled("Integration test is demonstrated via curl commands once the app is running")
    void placeholder() {
    }
}
